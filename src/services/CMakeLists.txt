# MIGRATION to JANA2 2026.x: pytorch service retired (it was a copy-pasted logger,
# never provided torch functionality - see ai_rework/notes).
add_subdirectory(root_output)
add_subdirectory(log)
add_subdirectory(dqm)
# add_subdirectory(pytorch)   # retired
